export {};
// import { DispatchType, StoreType } from 'state/configureStore';

// type HandledActionTypes = {};

// export const appInitializationMiddleware = (store: StoreType) => (next: DispatchType) => (action: HandledActionTypes) => {
//     // if (action.type === CommonActionTypes.WebSocketInitializationSuccess) {
//     //     //store.dispatch(initializeApplication(action.user));
//     //     return next(action);
//     // }
//     return next(action);
// };
